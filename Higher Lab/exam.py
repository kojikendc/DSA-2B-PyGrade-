def calculate_exam_scores():
    print("\n--- Exam Component ---")
    scores = [float(input(f"Enter score for the Exam: "))]
    total_items = [float(input(f"Enter highest possible score for the Exam: "))]
    return scores, total_items
